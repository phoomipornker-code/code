# Proven PV reference

Field-validated Boost firmware tag used as guideline:

`cv58-stability-v14-cv-stable`

Boost SoftStart→CC_MPPT→CV→DONE in `../firmware.ino` is frozen to that behavior
(see `FW_VERSION_TAG = cv58-boost-v14-forward-v25`).
